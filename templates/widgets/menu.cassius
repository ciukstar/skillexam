
 ##{idOverlay}
     z-index: 101

##{idDialogMainMenu}
    z-index: 102
    header
        nav
            flex: 1
            
            ##{idButtonMainMenuClose}
                margin-left: auto

    nav.drawer > a:is(:hover)
        background-color: var(--active)

    nav.drawer > a:is(.active)
        background-color: var(--secondary-container)
        color: var(--on-secondary-container)