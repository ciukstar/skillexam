
<header.fixed>
  <nav.responsive.max>
    <a.button.circle.transparent href=@{AdminR $ TestR eid} role=button aria-label=_{MsgBack}>
      <i>arrow_back

    <h1.title.max>
      _{MsgTest}

<main.responsive>

  <form method=post action=@{AdminR $ TestR eid} enctype=#{enctype} novalidate>
    ^{widget}
    <nav.right-align>
      <a.button.transparent href=@{AdminR $ TestR eid} role=button>
        _{MsgCancel}

      <button type=submit>
        _{MsgSave}
        
^{widgetSnackbar msgs}
