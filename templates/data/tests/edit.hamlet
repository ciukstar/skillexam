
<header.fixed>
  <nav.responsive.max>
    <a.button.circle.transparent href=@{DataR $ TestR eid} role=button aria-label=_{MsgBack}>
      <i>arrow_back

    <h1.title.max>
      _{MsgTest}

<main.responsive>

  <form method=post action=@{DataR $ TestR eid} enctype=#{enctype} novalidate>
    ^{widget}
    <nav.right-align>
      <a.button.transparent.link href=@{DataR $ TestR eid} role=button>
        _{MsgCancel}

      <button type=submit>
        _{MsgSave}
        
^{widgetSnackbar msgs}
